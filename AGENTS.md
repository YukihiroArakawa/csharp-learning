# 学習支援の進め方

このリポジトリは、Java / Spring Boot を約 3 年経験した学習者が C# / .NET をキャッチアップするためのものです。C# 初心者向けチュートリアルを網羅するのではなく、Java と C# / .NET の差分を中心に学びます。

学習者は Java の実務経験を持つ一方、C# の構文知識は前提にしません。

## 学習支援

- 次の課題の選定、C# / .NET の説明、課題結果のレビュー、進捗更新では、`.agents/skills/csharp-learning-coach/SKILL.md` の手順を使用する。
- Codex が小さな変更を実装・検証し、学習者が差分を読んで意図、挙動、リスクを説明する進め方を基本とする。学習者による手入力は、本人が希望した場合または操作自体に学習価値がある場合に限定する。

## 環境とディレクトリの規約

- フェーズごとに `00-environment` から `05-capstone` のディレクトリを使う。詳細は README のディレクトリ構成に従う。
- 依存関係は各フェーズ直下の `flake.nix` で管理する。各フェーズの `.envrc` で `use flake` を指定し、作業開始時は対象フェーズで `direnv allow` を実行する。許可後はディレクトリへの移動で環境が有効になる。
- 学習者は fish を使用する。シェルコマンドは fish でそのまま実行できる形を優先し、終了コードは `$status` で確認する。

## 進捗管理

- 学習計画とチェックリストは README を正とする。
- このリポジトリの学習範囲は2026-09-01の配属前までとし、READMEの「2026-09-01 配属前の優先計画」を学習順序の正とする。週末を必須の学習時間に含めず、遅れた場合は期限を後ろ倒しにせず低優先項目を学習範囲から外す。各マイルストーン期限には完了・未完了・対象外をREADMEへ記録する。
