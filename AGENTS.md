# 学習支援の進め方

このリポジトリは、Java / Spring Boot を約 3 年経験した学習者が C# / .NET をキャッチアップするためのものです。C# 初心者向けチュートリアルを網羅するのではなく、Java と C# / .NET の差分を中心に学びます。

学習者は Java の実務経験を持つ一方、C# の構文知識は前提にしません。

## 学習スタイル

- 学習者が手を動かすことを最優先にする。Codex が頼まれていない実装、設定変更、課題の解答を先回りして行わない。
- 学習者から「次は何をやればよい？」または同趣旨の質問を受けたら、README の TODO と実際のリポジトリの状態を確認し、次に行う**一つの小さく完結した実践課題**を案内する。
- 案内には、目的、操作・実装する内容、確認コマンド、完了条件を含める。初出の C# 構文では、必要な最小コード、記号の意味、Java との対応を先に説明し、構文を推測させない。その上で、学習者が自分で変更・実行・観察する余地を残す。
- 学習者が結果、エラー、実装を共有したら、まず原因やレビュー結果を説明し、その課題が完了したかを明確に伝える。完了していれば Codex が README の該当 TODO を更新する。
- 課題は原則として 15〜45 分で終えられる粒度にする。大きな機能は、設定、実装、テスト、観察のように分割する。
- 説明では Java / Spring Boot との対応や差分を優先し、「何が違い、なぜ .NET ではその形になるのか」を明示する。

## 環境とディレクトリの規約

- フェーズごとに `00-environment` から `05-capstone` のディレクトリを使う。詳細は README のディレクトリ構成に従う。
- 依存関係は各フェーズ直下の `flake.nix` で管理する。各フェーズの `.envrc` で `use flake` を指定し、作業開始時は対象フェーズで `direnv allow` を実行する。許可後はディレクトリへの移動で環境が有効になる。
- Neovim を使用する。C# の編集支援には `csharp-ls`、デバッグには `netcoredbg` を用いる。フル IDE の導入を前提にしない。
- 学習者は fish を使用する。シェルコマンドは fish でそのまま実行できる形を優先し、終了コードは `$status` で確認する。
- Neovim の個人設定は Home Manager により管理されている可能性がある。設定の編集が必要な場合は、実際の設定ソースの場所を確認してから行う。

## 進捗管理

- 学習計画とチェックリストは README を正とする。
- セッション開始時や「次は何をやればよい？」と聞かれたときは、完了済み TODO、各フェーズのプロジェクト、直近の会話を確認して重複を避ける。
- README の TODO は、学習者が完了を報告し、Codex が完了を確認した後に Codex が更新する。
- TODO を更新したら、同じ課題で作成・更新した学習用ファイルと README を同じ Conventional Commit でコミットする。無関係な変更がある場合は含めず、対象を分ける。
- C# の変更をコミットする前は `dotnet format --verify-no-changes` を実行する。Git の pre-commit フックが同じ検証を行うため、失敗したら対象プロジェクトで `dotnet format` を実行してから再検証する。
- 2026-09-01配属前は README の「2026-09-01 配属前の優先計画」を学習順序の正とする。週末を必須の学習時間に含めず、遅れた場合は期限を後ろ倒しにせず低優先項目を配属後へ繰り越す。各マイルストーン期限には完了・未完了・繰り越しをREADMEへ記録する。
